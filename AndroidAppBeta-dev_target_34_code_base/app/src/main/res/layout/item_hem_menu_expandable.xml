<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="@dimen/screen_padding"
        android:gravity="center_vertical">

        <ImageView
            android:id="@+id/imgIcon"
            android:layout_width="@dimen/_24dp"
            android:layout_height="@dimen/_24dp"
            tools:src="@android:drawable/ic_menu_more"
            android:contentDescription="icon"/>

        <Space
            android:layout_width="@dimen/screen_margin"
            android:layout_height="@dimen/_0dp"/>

        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            style="@style/CustomTextViewHemMenu"
            tools:text="Sub Menu"/>

        <Space
            android:layout_width="@dimen/screen_margin"
            android:layout_height="@dimen/_0dp"/>

        <ImageView
            android:id="@+id/imgExpand"
            android:layout_width="@dimen/_14dp"
            android:layout_height="@dimen/_14dp"
            android:src="@drawable/ic_dropdown_arrow_blue"
            android:contentDescription="expand"/>
    </LinearLayout>

    <!-- bottom divider -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@android:color/darker_gray" />

    <LinearLayout
        android:id="@+id/layoutSubOptions"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:visibility="gone"
        android:paddingStart="48dp">
        <!-- Sub-options will be added dynamically or via adapter -->
    </LinearLayout>

</LinearLayout>
